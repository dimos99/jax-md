[report]
exclude_lines =
  @abc.abstractmethod
  raise NotImplementedError

[tool:pytest]
markers =
  slow: long-running validation tests
  addopts = -m "not slow" # Exclude slow tests by default. Use `pytest -m slow` to run them.
